{
	// Renamed default module frogatto to frogatto4, because we don't want to choke on an old savefile from 1.3 or vice-versa.
	// List can also include such as "--width", "1024", "--height", "768", "--bigscreen", "--proportional-resize", etc.
	arguments: ["--module=frogatto4", "--no-tests" ],
}
