{
	properties: {
		mytest: "decimal<- x*x",
		mydetails: "def() ->[decimal] [x,y,z]",
	},

	handlers: {
		process: "[if(cycle%50 = 0, set(animation, 'walk')), if(cycle%250 = 0, set(animation, 'strike')), set(x, x+0.01), debug('I am moving: ' + x + ' ' + mytest + ' ' + mydetails())]"
	},
}
